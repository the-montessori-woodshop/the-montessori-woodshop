version: "3.7"

services:
  redis:
    image: "redis:alpine"

  postgres:
    image: alpine:latest
    build: ./postgis
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - "./pgdata:/var/lib/postgresql/data"
