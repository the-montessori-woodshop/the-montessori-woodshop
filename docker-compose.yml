version: "3"
services:
  backend:
    build:
      context: ./packages/medusa-backend
      dockerfile: Dockerfile
    depends_on:
      - postgres
    ports:
      - "9000:9000"
    command: bash -c "yarn seed && yarn start"
    volumes:
      - .:/backend/db
      - /backend/medusa/node_modules
    env_file:
      - ./.env

  postgres:
    image: postgres:14
    ports:
      - "35432:5432"
    env_file:
      - ./.env

  redis:
    image: redis
    expose:
      - 6379
    env_file:
      - ./.env

  admin:
    build:
      context: ./packages/medusa-admin
      dockerfile: Dockerfile
    env_file:
      - ./.env
    ports:
      - "7000:80"
