{
  "name": "@woodshop/api",
  "version": "1.0.0",
  "description": "The supporting API and Database Schema for The Montessori Woodshop storefront",
  "scripts": {
    "client:build": "NODE_ENV=production yarn node ./scripts/build-client.mjs && tsc -p tsconfig.client.json",
    "client:dev": "NODE_ENV=development yarn node ./scripts/build-client.mjs & tsc -p tsconfig.client.json",
    "publish": "wrangler publish",
    "api:build": "yarn generate && yarn node ./scripts/build.js",
    "build": "yarn api:build & yarn client:build",
    "generate": "PRISMA_CLIENT_ENGINE_TYPE=dataproxy && prisma generate",
    "migrate-dev": "prisma migrate dev",
    "migrate": "prisma migrate status && prisma migrate deploy",
    "push": "prisma push",
    "dev": "yarn client:dev & wrangler dev --ip 127.0.0.1 --port 8888"
  },
  "main": "dist/worker/worker.js",
  "exports": {
    ".": "./dist/worker/worker.js",
    "./client": {
      "types": "./dist/client/client/index.d.ts",
      "import": "./dist/client/client.js",
      "default": "./dist/client/client.js"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/the-montessori-woodshop/the-montessori-woodshop.git"
  },
  "author": "Drew DeCarme (drew@themontessoriwoodshop.com)",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/the-montessori-woodshop/the-montessori-woodshop/issues"
  },
  "homepage": "https://github.com/the-montessori-woodshop/the-montessori-woodshop#readme",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "@cloudflare/wrangler": "^1.19.8",
    "@esbuild-plugins/node-modules-polyfill": "^0.1.4",
    "esbuild": "^0.14.25",
    "esbuild-plugin-alias": "^0.2.1",
    "prisma": "3.10.0",
    "typescript": "next"
  },
  "dependencies": {
    "@prisma/client": "3.10.0",
    "itty-router": "^2.5.2"
  },
  "installConfig": {
    "hoistingLimits": "workspaces"
  }
}
