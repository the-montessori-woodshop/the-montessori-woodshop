import { styled } from "@linaria/react";

import { makeRem } from "../../../theme";

export const TopNavLogo = styled.img`
  border: none;
  background: none;
  max-height: ${makeRem(48)};
  width: auto;
`;
