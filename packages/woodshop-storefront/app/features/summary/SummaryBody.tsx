import { makeRem } from "@woodshop/components";
import { PageLayoutPaneBody } from "~/components/page-layout";
import styled from "styled-components";

export const SummaryBody = styled(PageLayoutPaneBody)`
  padding: 0 ${makeRem(24)};
`;
