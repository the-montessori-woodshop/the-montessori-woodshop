import { TypographyCopy } from "@woodshop/components";
import styled from "styled-components";

export const EmptyStateSubtitle = styled(TypographyCopy).attrs({
  cxColor: "textSecondary",
  cxNode: "div",
})`
  text-align: center;
`;
