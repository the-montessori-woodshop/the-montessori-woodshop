import { TypographyHeading } from "@woodshop/components";
import styled from "styled-components";

export const PageLayoutPaneHeaderTitle = styled(TypographyHeading).attrs({
  cxVariant: "h3",
  cxNode: "h2",
})``;
